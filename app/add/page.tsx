import Form from "@/components/form";
import dbConnect from "@/db/db";
import UserModel from "@/model/user";

const Add = async () => {
  return (
    <div>
      <Form />
    </div>
  );
};

export default Add;
